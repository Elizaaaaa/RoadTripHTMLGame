# 网页游戏框架设计 v1.1（引擎向）

> 本文档用于讨论阶段，确定后将作为 Claude Code 项目里的 `design-doc.md`，指导开发。

## 0. 范围界定（重要）

**本项目只交付引擎（engine）和内容数据结构模板，不代写正式故事文案。**

- 之前讨论中出现的"雾岭镇""失踪者播报"等设定，仅作为**示例/占位内容**，用来验证引擎功能是否好用，不是最终交付的剧本
- `content/` 目录下会保留 3 天的示例数据（结构完整、字段齐全），供你参照格式自己填真实故事和文案
- 所有下面提到的系统（理智值、关键词高亮、事件复盘、播报等）设计的是**通用规则和数据结构**，具体文案、线索内容、人物、地点名称均由你自己决定并填入
- 引擎侧会尽量做到"改数据不改代码"——你写故事时大概率只需要编辑 JSON/文本文件，不需要碰引擎代码

---

## 1. 世界观与主线（已确认）

**类型**：COC 风格调查向悬疑游戏，公路旅行 + 废弃小镇找真相题材，网页轻量呈现，不含完整跑团掷骰体系（局部掷骰事件见第 5.4 节）。

**表面前提（玩家视角看到的）**：两名网红好友开车做美国公路旅行，途中车没油，"碰巧"导航到了白湖镇入口一座废弃加油站——从这里徒步进镇。**镇内没有稳定信号，只有加油站附近能稳定联网**。两人架起手机把这段"废镇探秘"过程拍成 vlog 边拍边发，起初只是猎奇打卡，深入调查小镇废弃原因的过程中一步步揭开真相。玩家操作这两人组成的调查小队——白天在镇内探索取证、天黑前必须走回加油站剪辑发布当日素材——能否活着逃出小镇，取决于三件事：**能否每天在午夜前剪辑发布并回旅馆**、**关键线索收集得全不全**、以及收齐之后在废弃工厂那一次**高潮抉择**怎么选（结局判定见第 5.3 节的五结局模型）。

**核心真相（幕后，不直接告诉玩家，靠线索一步步拼出）**：白湖镇的废弃并非经济衰败——约一万年前，一块具有生命的陨星矿物降落在白湖位置，携带**格拉基**的一小块神经组织（类似种子/胚胎的大脑），后来的调查员将其命名为**湖心刺核**。它利用湖中的硼砂、碳酸盐和金属元素生长出结晶神经，把整片地下卤水变成自己的感知器官，但白湖后来迅速干涸，胚种没能获得足够生物质，陷入休眠，蒸发后的高浓度盐层反而把它封在地下。一战期间硼矿大规模开发，白湖周边建起工厂（即已确认的 canon **White Lake Alkali Company**，见 `content/worldbuilding.md`）并涌入居民形成小镇；工厂持续抽取卤水改变地下压力，破坏了盐层封印，热水与废卤水回灌为晶体网络带来能量，工厂管道把污染的卤水输送到全厂区，格拉基的力量悄无声息污染了矿区。直到 **1982 年**，湖心刺核苏醒，上百名镇民被污染成**眷属**。一队调查员（多人，不是两人小队）赶到白湖镇，此时湖心刺核已吸收太多能量、即将苏醒成为格拉基的化身——情况紧急，调查员借助**尤格索格斯**的力量在工厂里制造了一个简易的**子午线仪**，强行把整个白湖镇的时间剥离出来封印保存，使白湖镇被从正常时间里整体剪切出去，永远循环重复灾难前的最后那几天。被封印的是**真实的白湖镇连同全部镇民**——整座镇子连人带物一起被剪进时间裂隙。现实世界这一侧留下的只是这座小镇的一个**投影**：地理、建筑、物件都还在，人不在（双层结构与两层之间的耦合规则见下面 1.1 节）。因其神秘学属性，大部分普通人会下意识无视掉这个投影所在的位置，几十年来几乎无人到访——这也是主角发布的 vlog 没人当真、评论区开始出现诡异留言的幕后原因（呼应第 3.3 节"信号闪现"机制）。两名主角这次"意外"没油抛锚在这里，表面是巧合，实际是**格拉基外泄的力量悄悄影响了她们的选择/路线**，把她们引到了这里。

下面 1.1-1.5 节把这段总述拆成可直接改写成线索/词条的细节（来源：`content/drafts/outline.md` 2026-09-11 大纲）。**这些是幕后真相，玩家只能通过碎片拼出，不要在任何事件正文里整段直说。**

### 1.1 世界的两层结构：裂隙里的真实小镇 / 现实层面的投影

**这是全篇最需要先立住的设定，1.2 之后的一切都建立在它上面。**（来源：2026-09-11 对话补充）

1982 年子午线仪封印的，是**真实的白湖镇连同全部镇民**——整座镇子被剪进**时间裂隙**，在里面永远重复灾难前的最后几天。现实世界这一侧留下的，是这座小镇的一个**投影**：

- **玩家走的整张地图都是投影**。镇上看不见人，不是因为人都搬走了（那只是对外说法，见 1.2），而是因为人在另一层。
- 投影带着小镇的神秘学属性，**大部分人会下意识忽略它所在的位置**，所以几十年来几乎无人到访——这同样是 vlog 没人当真、评论区开始出现诡异留言的根源（呼应 3.3 节）。QQ/BB 这次能"碰巧"开到这里，是格拉基外泄的力量在起作用，属于例外而不是常态。
- **两层之间靠实体物品耦合**：一件实体物品若同时存在于两层，**真实小镇里的居民对它做出的改变，会呈现在现实层面的投影上**。这是玩家唯一能接触到"那边"的通道，也是本作的核心叙事机制。
- **封印正在衰减（重要，决定第 3 天的紧迫感）**：到 2019 年，1982 年那套土法封印**本身已经非常薄弱**，真实小镇对现实层面的影响越来越大。所以这条耦合通道不是一直这么好用的，它是**裂痕扩大的症状**——房间越来越干净、纸条越来越顺畅、评论区越来越不对劲，这些在玩家看来是"线索变多了"，实际上是倒计时。裂痕继续扩大下去的终点就是格拉基降临，这也是第 3 天两条抉择路线共同的前提：不作为本身也是一种选择。

**这条规则已经在解释的现象（写线索时直接用）**：

| 玩家在投影里看到的 | 实际发生的事 |
|---|---|
| 大部分房间干净整洁，不像废弃几十年 | 那边的镇民照常打扫、照常生活 |
| 沙狐旅馆无人却能自助入住，柜台留着说明（Day 1 已实装） | 那边的旅馆还在正常营业 |
| 入住登记册上有人手写"夜里不要靠近这里，零点前一定要回旅馆"（`archive.json` 的 `alkaliWorks` 词条） | **那边某个镇民留给她们的警告**——这条原本的占位文案现在有了真实来源，保留不改 |
| 第 2 天一早，QQ/BB 可以用**纸条留言**跟镇民沟通 | 双向通信在此打通，是第 2 天的关键机制（待 day2 草稿落实） |

**由这条规则推出的两个要点**：
- **子午线仪本体留在现实层面**（1982 年就是在现实世界的工厂里造的，封印的是镇子不是它自己），所以玩家真的能走到它面前——这是整个封印唯一的物理弱点，也是第 3 天那次高潮抉择（结局③/④/⑤，见 5.3 节）在机制上成立的前提。
- **"与时间缝隙里的小镇连接上"** 指的就是把纸条这条通道走通；**高潮抉择里"小镇居民的诱惑"**（劝她们砸掉子午线仪、劝她们开那道口子）也从这条通道传来——说话的声音来自另一层。

**裂隙里的"镇民"全部是 1.4 节的盐刺行尸**（已确认）——没有任何未被污染的人留在里面。这一点决定了纸条机制的底色：**纸条那头写字的每一个"人"都是眷属**，它们表现出的日常（打扫房间、经营旅馆、好心提醒夜里别靠近工厂）不是伪装出来骗人的，而是循环里被固定下来的生活本身；但它们要什么、为什么最后会劝 QQ/BB 动手，玩家在最后一刻之前无从判断。写纸条往来时不要让它们说话露馅太早——恐怖点在于"这些字迹读起来完全正常"。

### 1.2 1982 年事故：对外说法 vs 实际经过

**对外说法（镇外档案、旧报纸、论坛老帖能查到的版本，也是玩家一开始会接受的版本）**：矿场发生塌方 / 化学泄漏事故，工厂随之大规模裁员，小镇疏散，大部分居民搬走。

**实际经过（按时间顺序，可作为主线线索的拆分依据）**：
1. 工厂为提高产量，**加深了卤水抽取井**；
2. 钻头穿透盐层，**触及胚种外围的晶体牢笼**（即封住湖心刺核的那层高浓度盐封印）；
3. 数名矿工在地下听见**“湖水拍岸”的声音**——但白湖早已干涸；
4. 矿工开始**梦游，在夜间走向盐湖**；
5. 部分人的**脊椎或胸口长出黑色矿物刺**；
6. 工厂回收的卤水中出现**无法归类的有机组织**；
7. 胚种开始利用这些矿工，构筑自己的**第一批眷属**。
8. 眼看格拉基即将降临、后果不可逆转，一队调查员召唤奇迹，**把整个镇子的时间剥离出去**，永远停滞在邪神即将降临的那几天。

> 写作提示：1-7 是一条天然的"越查越不对劲"的递进链，很适合按天切分投放——前期给 1、2（工业事故层面，看起来还合理），中期给 3、4（超自然但可解释成集体癔症），后期给 5、6、7（不可辩驳的身体异变）。

### 1.3 封印装置：子午线仪（The Meridian Engine）

- **性质**：一件与**犹格·索托斯（尤格索格斯）**有关的造物，由 1982 年那队调查员现场改造而成。
- **构造（三样东西拼起来的土法装置，全部取自镇内，便于玩家在实地找到对应残骸）**：矿场中发现的**异常晶体** + **铁路信号设备** + 工厂的**压力仪表**。
- **它做了什么**：没有消灭胚种，而是把整个 White Lake 从正常时间里**“剪切”**出去。
- **封印的五条效果（每一条都是可直接转成游戏内可观察现象的钩子）**：
  | 效果 | 玩家能观察到的样子 |
  |---|---|
  | 镇内永远重复 1982 年灾难前的最后几天 | 日历/报纸/店铺告示停在同一批日期；有些场景像是被"重演"过很多遍 |
  | 外界仍正常走向 2019 年 | 主角的手机、vlog 发布时间、评论区都属于 2019 年，与镇内一切对不上 |
  | 普通旅客会无意识绕开小镇 | 呼应第 3.3 节：vlog 没人当真、路过的人从没提起过这里 |
  | 地图、电话和道路会把人引回原处 | 天然解释"为什么走不出去/为什么只有加油站有信号"，也是限制玩家离开的叙事理由 |
  | 镇民不能真正衰老，也无法真正死亡 | 与 1.3 节的眷属特征互相印证：不流血、破裂后不是正常器官 |
- **现在在哪**：子午线仪**仍存放在废弃工厂（`alkaliWorks`）的地下**。
- **工厂旁的大坑（`theSink`）**：1982 年事故中调查员们与眷属交战留下的痕迹，不是采矿坑也不是自然塌陷。

### 1.4 眷属：盐刺行尸

被湖心刺核污染的镇民。按 1.1 节的双层结构，它们和整座镇子一起在时间裂隙那一侧，**玩家在投影里见不到它们本人**，平时看起来仍像普通镇民，但身体有明显异常：

- 皮肤干燥发白，像覆盖着**盐霜**；
- 伤口**不流血**，只渗出**浓稠卤水**；
- 背部或胸腔内生长**黑色金属刺**；
- **强光下**能看见皮下存在细小晶体；
- 说话时偶尔会**混入很久以前某位死者的声音**；
- 身体破裂后，内部不是正常器官，而是**盐晶包裹的半透明组织**。

> 写作提示：**这六条不能靠直接目击来投放**——玩家在投影里看不到镇民本人。主要来源应是 1982 年留下的记录（工厂医务记录、调查员笔记、照片底片、验尸/化验单）和跨层泄漏的痕迹（纸条上的卤水渍、擦不掉的盐霜手印、录音里混进来的第二个声音）。这六条按"隔多远才看得出来"排序正好是一条渐进的恐怖曲线：盐霜、强光下的皮下晶体属于照片上就看得见的（低 `sanityCost`）；死者的声音、伤口不流血属于近距离记录（中等）；内部的盐晶组织只出现在最坏的材料里（高 `sanityCost`，适合掷骰事件的 `critFail`/`critSuccess`）。真正的直接目击留到高潮/结局附近再用，才有分量。
>
> 高潮抉择里"**小镇居民的诱惑**"（第 1 节、5.3 节）指的就是它们——对玩家而言前期它们只是"这个镇子居然还有人"，真相揭开后才知道劝她们动手的是什么。

**特例：那个婴儿（结局⑤的核心，全篇最大的一次反转）**

眷属里有一个**在 1982 年事故发生时刚刚出生的婴儿**。按封印规则（镇民不能真正衰老），37 年过去他仍然是个婴儿。

- 镇民会告诉 QQ/BB：**这个孩子出生时事故已经发生，他没有被污染**，是镇子里唯一无辜的存在。
- 这是假的，而且比"他其实也是眷属"更糟——**这个婴儿是格拉基胚种的载体**。湖心刺核用三十七年等到的不是苏醒，是**一个能被带出封印的容器**。
- 玩家在游戏过程中不该有机会证伪这套说法：婴儿身上显不出 1.4 节那六条特征（太小），而 QQ/BB 在投影这一侧根本见不到他本人，只能听见纸条那头的描述。**这正是设计意图——玩家必须在信息不足的情况下做那个选择。**
- 设计意义：它给"诱惑"一个玩家真的会心软的具体形状。抽象的"放我们出去"容易拒绝，"至少把孩子带走"不容易。

**结局④和⑤的完整链条（已确认，按 `content/drafts/outline.md` 更新）**：这两个结局共用前三步，只在最后那一道口子上分岔。

1. 到 2019 年封印本身已经很薄弱，裂痕继续扩大下去就是格拉基降临（见 1.1 节"封印正在衰减"）——**什么都不做也救不了任何人**，这是这两个结局成立的前提；
2. QQ/BB 没有选择摧毁子午线仪，而是**把它修好、把封印重新加固**。这不是"保持现状"：裂隙本来就已经不稳，重新收紧的力道会把里面的东西连同承载它们的那几天一起碾碎——**修复即摧毁**，镇民清楚，两人也被告知了；
3. 镇民接受这个结果（他们本来就活在同一天里，"自愿牺牲"这条立场不变），**唯一的条件是在封印合拢之前撕开一个口子**，把小镇里"唯一无辜"的婴儿送出去；
4. **拒绝那道口子 → 结局④「旧日应当留在过去」**：封印在古神将临的关键时刻合拢，小镇与全部居民在不稳定的时间裂隙里被彻底摧毁，现实世界记录到一次地震。胚种随之一起碎掉——这是五个结局里**唯一真正拦下格拉基的一条**，代价是一镇子的人，而且两人永远不会知道自己拦下了什么；
5. **接受那道口子 → 结局⑤「于毁灭中拯救」**：镇子照样消失，但 QQ/BB 以为自己既化解了危机、又救下了一个幸存者——**实际上胚种已经随婴儿出了封印，真正的邪神依旧会降临。**

**结局⑤必须写成两段式（已确认）**：

- **第一段·结局正文 = 一场真正的胜利**。完全站在 QQ/BB 的视角：危机解除了，镇子的苦难结束了，怀里还抱着一个活下来的孩子。**这一段不留任何破绽**——不要写"她总觉得哪里不对"，不要给暗示性的镜头，不要让文字比主角知道得更多。玩家读完应该真心以为自己打出了好结局，那份松了一口气是必须给足的，不然后面那一下不疼。
- **第二段·后日谈 = 揭晓**。婴儿的真相放在这里，和玩家隔着一次点击（`content/endings.json` 的 `salvation_in_ruin.epilogue`）。具体揭到什么程度（明说胚种载体，还是只给一个足以让玩家自己拼出来的画面）留给正式文案，但揭晓的位置已经定了：**不在正文里，在后日谈里**。

> **实装状态：✅ 已完成**。`content/endings.json` 每个结局可选配 `epilogue`（后日谈，本身也能用换行继续分页），`engine/ending.js` `getText()` 把它跟正文分开返回，`engine/main.js` `showEnding()` 按"正文各页 → 后日谈各页"顺序播出，翻到后日谈那页标题换成「后日谈」，重开按钮只在最后一页给。正文和后日谈在数据结构上就是分开的两段，作者没法不小心把翻转写进正文最后一行。演出上标题逐字上浮、正文逐句淡入（`engine/reveal.js`）。

**镇民的立场（已确认）**：它们**完全清楚自己在做什么**。整场合作是一次**自愿的牺牲**——镇民明知重新加固封印就是让自己连同整座镇子一起彻底消失，仍然主动配合，唯一的目的就是哄骗 QQ/BB 完成邪神降临的最后一步：把胚种送出封印。眷属对格拉基的忠诚高于自身存续，"救孩子"这个条件从头到尾是设计好的。

这条决定了第 2-3 天所有纸条的写法——**它们不是在临时起意，是在做一场长局**：

- **前期的善意是投资**。旅馆登记册上"夜里不要靠近这里，零点前一定要回旅馆"那句手写提醒（`archive.json` 的 `alkaliWorks` 词条）不是好心，是**建立可信度**——正因为"它们提醒过我们、救过我们一次"，第 3 天那个关于婴儿的说法才会被相信。所有前期示好都要经得起这样回看。
- **它们不会主动推销那个孩子**。真正的骗局不会显得急切：婴儿的存在应该是 QQ/BB **自己一点点问出来的**，最好还带着镇民"本来不想说"的犹豫。玩家事后回想才会意识到，每一步都是被引着走的。
- **提议由镇民开口，选择权留给玩家**（已按大纲修订，取代早前"必须让两人以为是自己想到的"那条）。第 3 天在子午线仪跟前，镇民同时摆出"砸了它"和"修好它"两条路，显得公允、甚至像是把命交到两人手里——反讽的落点因此挪了位置：**它们真正想要的从来不是这一问的答案，而是修复那一支后面追加的那道口子**。第一问越像一次坦诚的二选一，第二问那句"至少把孩子带走"就越不像请求。所以第一问的文案要写得两边都真诚，别让"修复"显得是它们偏好的答案。
- **重看前两天要能看出破绽，但只能在重看时看出**。这是给二周目和高进度玩家的：同一批纸条，知道真相后读起来完全是另一回事。

> **已回答（大纲确认）：时间裂隙里的小镇是被"重新加固封印"这个动作本身摧毁的**。早前这里挂着一个待定项——两人只能在投影这一侧行动，怎么才能毁掉另一层的小镇。大纲给的答案不需要额外机制：**修复子午线仪就是摧毁**。裂隙已经不稳，把它重新收紧的力道会把里面的东西连同那几天一起碾碎，而两人在现实这一侧、在工厂地下，够得着那台机器。由此顺带定下的几件事：
> - 它和破坏子午线仪（结局③）确实是两条相反的路：③把镇子**放回**现实，④⑤把镇子**碾碎在裂隙里**；
> - **湖心刺核本体的下场**跟着一起定了：刺核封在裂隙那一侧，跟镇子一起碎——所以结局④是真的拦下了格拉基，也所以结局⑤里胚种非得靠婴儿这条路先出来不可；
> - 还需要正式文案补的只剩一件：镇民把"修复=你们要杀光我们"这件事讲得多明白。讲得越明白，④越疼、⑤越像慈悲。

### 1.5 真相如何落到现有地图上

| 地点（`assets/maps/hotspots.json`） | 承接的真相 | 现状 |
|---|---|---|
| `alkaliWorks` 废弃工厂 | 加深抽取井的元凶；子午线仪就封存在其地下 | 已有档案库词条（占位文案），暂无事件 |
| `theSink` 大坑 | 1982 年调查员与眷属交战的战场遗迹 | 已有词条，正文是占位的"坑底一排鞋" |
| `lake` 白湖 / 干湖 | 湖心刺核就在湖底盐层之下；矿工梦游的去向 | 已有词条 |
| `museum` 博物馆 | "带孩子体验采矿工人的一天"——官方版事故说法的最佳投放点 | 已有词条，暂无事件 |
| `library` 图书馆 | 旧报纸/镇史/工厂档案，对外说法与实际经过的对照来源 | 已有词条，暂无事件 |

**游戏时间线**：已确认设定在 **2019 年 7 月 3 日-7 月 5 日**，对应固定 3 天结构（第 2 节）：第 1 天=7 月 3 日晚（20:00-24:00，抛锚+初见小镇），第 2 天=7 月 4 日全天，第 3 天=7 月 5 日全天（高潮日）。

**结局模型（已确认，按 `content/drafts/outline.md` 定为五个，判定细则见第 5.3 节）**：
1. **结局①·被黑夜吞噬**：任意一天没能在午夜前剪辑发布并回旅馆，当场失败结束，不进入下一天。这是唯一一条"过程中就可能终止"的路径。
2. **结局②·一无所知**：三天走完但关键线索没收齐（或收齐了却没去按那个抉择），平安开出小镇，收获几条烂 vlog。**这个结局不代表"什么都没发生"**：当晚封印会自行破碎，后果照常发生，只是两人和玩家都不会知道——她们只会在新闻里看到一条 7.1 级地震（见 1.6 节）。
3. **结局③·神的降临**：关键线索收齐后，在小镇居民的诱惑下选择**摧毁**子午线仪。连锁反应按顺序发生：**真实的白湖镇被重新塞回现实世界 → 全部眷属同时被解放 → 湖心刺核吸收所有眷属 → 格拉基分身显现**。注意第一步：破坏封印不是"把镇子毁掉"，而是把它放回来——上百个盐刺行尸连同整座镇子一起重新出现在 2019 年的现实里，两人最后看到的是白湖上逐渐破茧的胚种。
4. **结局④·旧日应当留在过去**：关键线索收齐后选择**修复**子午线仪，并拒绝在封印合拢前开那道口子。封印在古神将临的关键时刻被重新加固，小镇在不稳定的时间裂隙里被彻底摧毁，现实世界记录到一次地震。**五个结局里唯一真正拦下格拉基的一条**，代价是整座镇子，而且两人永远不会知道自己拦下了什么。
5. **结局⑤·于毁灭中拯救（表面最好、实则最黑的结局）**：同样选择修复子午线仪，但在镇民的劝说下于加固之前开一道口子，接住那个"唯一无辜"的婴儿。两人以为既化解了危机又救下了幸存者——**而婴儿正是格拉基胚种的载体，邪神依旧会降临**——这一层不在结局正文里揭晓，放在后日谈。完整链条与两段式写法见 1.4 节。

具体的逐天情节、线索、地点安排由你后续提供的详细大纲确定；下面第 2 节起的通用系统设计（时间、地图、理智、掷骰、复盘、发布等）保持不变，只是结局判定模型和主线真相按上面改写，`content/` 里现有的"违约金/看守人/胸针"占位剧情待新大纲到位后整体替换。

### 1.6 现实锚点：2019 年 Trona 地震（跨结局固定事件）

**已确认**：无论玩家走到哪个结局，现实世界**一定会记录到一次 6.4 级和一次 7.1 级地震**。这对应真实发生过的 2019 年加州 Ridgecrest / Trona 地震序列——**7 月 4 日的 M6.4 前震（当地时间上午 10:33 前后）** 和 **7 月 5 日的 M7.1 主震（当地时间晚上 20:19 前后）**，震中在 Searles Valley 一带，受创最重的就是 Trona。

这条设定有三个层面的作用：

**1. 它把游戏的三天钉死在真实历史上。** 第 2 天正是 7 月 4 日、第 3 天正是 7 月 5 日（见本节"游戏时间线"），两次地震恰好各落在一天的可探索窗口内：M6.4 在第 2 天上午（窗口 08:00-24:00），M7.1 在第 3 天傍晚（20:19，正好压在高潮抉择前后）。这不是要迁就的巧合，而是**整个三天结构原本就是照着这两次地震排的**。

**2. 它是封印衰减的物理签名，不是玩家行为的后果。** 因为五个结局里都会发生，所以地震**不能**写成"因为玩家做了 X 才地动山摇"——它是 1.1 节"封印正在衰减"那条暗线在现实世界留下的痕迹，玩家做什么都拦不住。叙事上这是前兆而非回报：M6.4 告诉玩家"另一层的事情开始影响这一层了"，M7.1 是封印最薄的那一刻。

**3. 同一份地震记录，五个结局五种成因**（已确认）——这是它最值钱的地方，可以在结局文案里直接兑现。外界看到的永远是同一条震波曲线，底下发生的事完全不同：

| 结局 | M7.1 实际是什么 |
|---|---|
| ① 被黑夜吞噬 | **封印持续衰减到自行破碎的那一刻**。两人早已不在了，那一下跟她们没有关系 |
| ② 一无所知 | 同上：**它自己碎了**。玩家什么都没做到，封印也撑不住了 |
| ③ 神的降临 | **她们亲手解放小镇的那一下** |
| ④ 旧日应当留在过去 | **封印重新合拢、把小镇碾碎在裂隙里的那一下**——唯一一次震动的成因是"有人拦住了它" |
| ⑤ 于毁灭中拯救 | 同④：**小镇在时间裂隙里消失时，投在现实层面上的那一下**。区别只在于有个襁褓先出来了 |

**结局②不是"什么都没发生"，是"什么都发生了，只是没人知道"。** 这一点要改写②的收尾写法：两人一无所获地离开，当晚封印自行破碎，后果照常发生，而她们和玩家都只会看到一条新闻里的震级数字。②的恐怖不再是"没查出真相"，是"真相根本不需要你查，它自己会来"。

**镇民并不知道封印会这么快碎掉**（已确认）——这一条看似是补充，实际上把它们那场长局整个翻了个面：

- 它们赌上整座镇子和自己的存在去骗 QQ/BB 把胚种带出去，**是因为它们以为还需要这么做**。而在结局①②③里，封印当晚就会破/被破，刺核照样能吸收所有眷属，**那场长局其实是多余的**。
- 唯一一个它们的算计真正起了作用的分支是**结局⑤**：只有在那里，封印被重新加固、小镇被提前碾碎、胚种没能等到裂隙自己碎开，才非得靠婴儿这条路出去不可。而**结局④证明这场长局输得起**——同样的局面，只要那道口子没开，它们就白等了三十七年。
- 写纸条时可以用上这份误判：**它们对时间的判断是错的**，可以让它们流露出"还有的是时间""再过几十年也未必"这类语气——玩家二周目回看会发现，它们比自己以为的更接近成功，也更接近毫无必要。

**4. 它是可被玩家验证的。** 这是本作最强的"细思极恐"钩子：玩家真的可以去查 2019 年 7 月 4 日和 5 日加州的地震记录，然后发现游戏说的日期、震级、时间点全都对得上。建议用已有机制自然地递给玩家，而不是旁白挑明——镇内没信号感受到震动，回加油站上网时新闻推送/评论区才确认发生了什么（3.3 节的信号闪现、加油站的"翻旧报纸/论坛老帖"都能承接）。

> **实装状态：✅ 已完成**。`content/days.json` 每天可选配一个 `timedEvents` 数组（`at` = 当天第几分钟），`engine/time.js` `crossedTimedEvents()` 在每次交互推进时间之后按左开右闭区间 `(推进前, 推进后]` 挑出被跨过的事件，`engine/main.js` `playTimedEvents()` 依次播完再接这个地点本来的事件。示例内容里 M6.4 配在第 2 天 `at:633`（10:33）、M7.1 配在第 3 天 `at:1219`（20:19），文案写的是"大约十点半"而不是精确到分——因为引擎每次交互固定推进 1 小时，实际会落在跨过该时刻的那一次交互上。
>
> **演出**：`engine/quake.js` + `ui.css`「地震演出」一节——按震级预设振幅/时长，抖动（`#app` 和弹窗两层不同步、振幅一路衰减）+ 盐碱色落灰 + 抖完一拍死寂，之后文案才逐句淡入。全程只动 transform/opacity 两个合成器属性，不碰 filter（理由见 ui.css 理智闪烁那一节的教训），演出期间落灰层接管点击，防止玩家在抖动中间又点一次地图。零美术素材。调参用 `tools/quake-preview.html`。
>
> **注意**：`at` 配得越晚，越依赖当天有足够多的可探索点位——引擎只在"去调查地点"时推进时间，当天点位不够就走不到 20:19。示例内容第 3 天只有 3 个事件，走不到 M7.1，这不算漏（见下一条：每个结局的后日谈里都有那句固定的新闻）。
>
> **另外（✅ 已落地）**：结局①可能在第 1 或第 2 天就提前结束，那时 M7.1 还没发生；结局③④⑤也可能在当天走到 20:19 之前就被玩家按下——所以两次地震还要能作为**结局文本里的固定收尾**出现（后日谈式的一句新闻），而不只是流程中的事件，这样"无论哪个结局都会记录到"才真正成立。现在 `content/endings.json` 里五个结局的 `epilogue` 都写了那两条记录（日期、时刻、震级、震中 Searles Valley），`god_arrival` 和 `past_stays_buried` 还额外配了 `quake` 字段，结局窗口弹出前先播一次 M7.1 的演出。

> 写正式文案前建议按 USGS 的公开记录核一遍精确时间、震级和震中表述，别把可验证的细节写错——这条设定的全部力量正来自它经得起查。

---

## 2. 体量与结构

- **总长**：已确认固定 **3 天**，每天的时间窗口不再是同一个常量：
  | 天数 | 时间窗口 | 说明 |
  |---|---|---|
  | 第 1 天 | 20:00 ~ 24:00 | 只有晚上，作为"刚发现小镇、天已经黑了"的开场，可探索时间短 |
  | 第 2 天 | 08:00 ~ 24:00 | 全天（早/中/晚一整段连续时间），探索窗口最完整 |
  | 第 3 天 | 08:00 ~ 24:00 | 全天，作为收尾/高潮日 |

  窗口起止分钟数由 `content/days.json` 每天数据里的 `startMin`/`endMin` 字段配置（不配置则兜底 08:00-20:00，见 `engine/time.js` 的 `DEFAULT_DAY_START_MIN`/`DEFAULT_DAY_END_MIN`），改天数/改每天时长都只需要改 JSON，不用碰引擎代码。
- **单日结构**（改为空间约束驱动，而非纯时间/菜单驱动）：
  `从加油站出发 → 镇内探索地点(消耗时间，偶有信号闪现收到粉丝留言) → 收集线索 → 赶在时间耗尽前走回加油站（或随时点顶部"开始剪辑"，两个入口是同一个编辑器）→ 剪辑台拖拽排时间轴、确认发布 → 播放量结算 + （若当天配置了正确顺序）对错判定 → 进入下一天`
- **原型机制重新命名对照**（架构沿用，仅换说法以贴合新设定）：
  | 原名称（占位设定） | 新名称（公路旅行设定） |
  |---|---|
  | ☎ 居民热线（主动查看，免费） | 📰 翻旧报纸/论坛老帖（主动查看，免费，每日一次，仅限加油站可用） |
  | ? 呼叫阿福 (−2H，主动触发) | 📶 信号闪现（被动随机触发，见第 3.3 节，无需玩家主动操作） |
  | 📡 夜间播报 & 收听率 | 🎥 回加油站发布 vlog & 播放量结算 |
- **主线线索 vs 支线线索**：
  - 每天至少 1 条"主线相关"线索（推动小镇真相），持续累积到一个跨天的"主案件库"，同时计入**探索进度**
  - 每天 1-2 条"当日支线"，独立小素材/小插曲，当天复盘、当天发布，增加节奏变化，避免每天都严肃烧脑
- **结局判定**：三条轴——午夜未归立刻判结局①；三天走完关键线索没收齐判结局②；收齐后由高潮抉择的两问决定结局③/④/⑤（详见第 5.3 节）。`choiceLog` 保留记录关键选择（剪辑复盘的 tag、抉择选项的 tag），只用于同一结局内的文案细节差异化
- **未能按时返回加油站的后果**：不分第几天，时间耗尽（走到当天 `endMin`）时如果还没点回加油站，直接判"被黑暗吞噬"坏结局（`night_madness`），游戏结束，不再进入下一天。判定时机是"十二点之后玩家再次尝试交互（点地图）的那一刻"，跨过十二点的那次交互本身还是正常走完，见 `engine/main.js` 的 `handleDayOver()`/`onHotspotClick()`。

### 2.1 调查次数预算与重要节点分布（已确认）

**先记住一条**：这个游戏的时间不是"流逝"的，是"花掉"的。`engine/time.js` 的 `TRAVEL_TIME_MIN = 60` 只在一种情况下被扣：玩家点了一个**当前真的有事件可触发**的调查地点。下面这些点击一律不花时间——

- 点加油站（`type:"basecamp"`，翻报纸/剪辑发布都免费）；
- 点一个事件已经被翻完的地点（`visitInvestigationSpot` 找不到未触发事件，原地不动）；
- 点一个事件被 `requires` 挡掉的地点（同上）；
- 顶部菜单的记事本 / 调查回顾 / 开始剪辑。

所以**一天有几个事件，这一天就有多长**。内容量直接等于时间轴，这是排节点时唯一要盯住的换算关系。

#### 每天的移动次数

| 天 | 窗口 | 总移动次数 | 建议排事件的格数 | 留给回程的格 |
|---|---|---|---|---|
| 第 1 天 | 20:00–24:00 | **4** | 3 | 1 |
| 第 2 天 | 08:00–24:00 | **16** | 15 | 1 |
| 第 3 天 | 08:00–24:00 | **16** | 16（高潮当场结束，不用回程） | — |

**为什么要留最后一格**：把时钟推到 `endMin` 的那一次移动会把 `pendingDayOver` 立起来，而 `onHotspotClick` 是在判断热点类型**之前**就检查这个标记的——也就是说此后点地图上的任何东西（**包括加油站**）都直接判 `night_madness`。顶部菜单的"开始剪辑"不走 `onHotspotClick`，所以那条路还能发布。这个不一致目前是活的：**用满 16 格的玩家，走地图回加油站会死，用顶部菜单发布则没事**。在决定怎么处理之前（见下方"待定"），按"最后一格是回程"排内容最安全。

#### 两次地震钉死了两个格子

`timedEvents` 按当天分钟数触发，而分钟数只被"有事件的移动"推动，所以地震的位置其实是**第几次移动**：

| 地震 | `at` | 落在第几次移动 | 含义 |
|---|---|---|---|
| M6.4 | 633（10:33） | 第 2 天**第 3 格**（600→660 跨过） | 之前必须有 2 格实事件，否则它永远不会响 |
| M7.1 | 1219（20:19） | 第 3 天**第 13 格**（1200→1260 跨过） | 之前必须有 12 格实事件 |

**这条是第 3 天的硬约束**：M7.1 要在流程里真的演出来，第 3 天就得排够 13 格以上的事件。排不够的话它只能靠结局后日谈里那句新闻兑现（`TODO.md` 已记这一条，不算漏，但高潮的分量会差很多）。

#### 建议的节点分布

地点是一个"事件队列"：同一个 `loc` 可以排好几条事件，每去一次消耗数组里的下一条。所以"分布"实际上是在决定**每个地点排几层**。二访、三访同一个地点正好吃到 1.1 节那条设定——两层世界通过实物耦合，**同一个地方第二次去，东西变了**，这不是填充，这是这套世界观最该被玩家亲手摸到的地方。

**第 1 天（3 格）——开场，不背关键线索的压力**

| 格 | 时刻 | 地点 | 节点 |
|---|---|---|---|
| — | 20:00 | 加油站 | 翻旧报纸（免费），点开 `[[中心广场\|square]]` 开出第一个地点 |
| 1 | →21:00 | 中心广场 | 纪念碑 + 布告栏，开出沙狐旅馆（现 `e1`） |
| 2 | →22:00 | 中心广场 | 更多告示，开出图书馆/博物馆/旧高中的词条（现 `e2`） |
| 3 | →23:00 | 沙狐旅馆 | 自助入住、拿 108 钥匙（现 `e3`） |
| 4 | — | 加油站 | 剪辑发布 → 回房休息 |

这一天已经排满了，**不要再加第 4 个事件**。如果还想给夜里一个钩子，把它挂在"回房休息"那一步（`openHotelNight`）而不是地图上——那一步不占移动次数。（引擎侧需要给 `openHotelNight` 支持一段按天配置的文案，小改动。）

**第 2 天（15 格）——纸条通道是主轴，曲线分四段**

| 格 | 时刻 | 节点 | 关键线索 |
|---|---|---|---|
| 1–2 | 08:00–10:00 | 开场。第 1 格必须是**旅馆**：前台登记册上的纸条是"另一层有人"的第一次接触（现 `e13`）。第 2 格给白天的镇子一个日常化的不对劲 | 0–1 |
| **3** | →11:00 | **M6.4 自动插播**，之后接这一格本身的事件。建议放图书馆：震完书架塌了，露出后面的东西——让地震"有用"，而不只是演出 | 1 |
| 4–7 | 11:00–15:00 | 官方说法 vs 实际经过。博物馆（"带孩子体验采矿工人的一天"＝官方版事故说法最好的投放点）、图书馆二访、老约翰酒馆、旧高中 | 1–2 |
| 8–9 | 15:00–17:00 | **纸条通道正式打通**。回旅馆，发现有人回了信；再去一次，变成一问一答。这两格是第 2 天真正的主线 | 1 |
| 10–12 | 17:00–20:00 | 追问。镇民有问必答，但绕开某些词。白湖/大坑的第一次接触 | 1 |
| 13–15 | 20:00–23:00 | 天黑之后的三格。夜间限定事件，`sanityCost` 最重的一批，镇民的语气开始不对 | 0–1 |
| 16 | — | 回加油站剪辑发布 | — |

**第 3 天（16 格）——收网，高潮压在末尾**

| 格 | 时刻 | 节点 |
|---|---|---|
| 1–4 | 08:00–12:00 | **补漏窗口**。前两天漏掉的关键线索在这里给第二个入口（见下方"关键线索怎么摊"） |
| 5–8 | 12:00–16:00 | 真相成型。大坑 / 墓地 / 干湖 三处交叉验证，1982 年那批调查员留下的痕迹 |
| 9–12 | 16:00–20:00 | **婴儿被"问出来"**。1.4 节的硬要求：镇民不能主动推销这个孩子，必须是 QQ/BB 一点点问出来的，最好还带着"本来不想说"的犹豫。全篇最需要写细的四格 |
| **13** | →21:00 | **M7.1**。封印最薄的那一刻 |
| 14 | →22:00 | 地震之后：废弃工厂锈死的铁门被**从里面**顶开——这一格才是"现在该去工厂了"的信号 |
| 15 | →23:00 | **废弃工厂 · 高潮抉择**（`e_finale`） |
| 16 | — | 缓冲格。玩家顺序走岔时的余量 |

> **配套改动（建议，尚未实装）**：现在 `alkaliWorks` 直接写在第 3 天的 `unlockedLocations` 里，也就是说一个前两天就把关键线索收齐的玩家，**第 1 格就能走进工厂按下抉择**，M7.1 根本来不及发生。把工厂改成由第 14 格那条"铁门被顶开"的事件用 `unlocksLocation` 开出来（跟第 1 天广场同一套机制），或者给 `e_finale` 的 `requires.clues` 加上那条事件产出的线索，高潮就不会被提前引爆。两种写法引擎都已经支持。

#### 关键线索怎么摊

结局③④⑤的门槛是 `linkedMainCase` 词条**全部**解锁（`FINALE_PROGRESS_THRESHOLD = 100`，见 5.3 节）。现在是 8 条，建议维持在 **8–10 条**，按 **1 / 3 / 4** 摊在三天上，并守住两条规则：

1. **第 1、2 天的关键线索只能挂在主线必经事件上**——不要挂在掷骰的成功档、不要挂在支线地点。玩家在第 2 天漏掉一条，到第 3 天是没有任何提示可以自查的，只会在结局窗口才发现自己被判了"一无所知"。
2. **前两天的每条关键线索，在第 3 天要有第二个入口**（正文里再出现一次那个 `[[显示文字\|key]]`）。第 3 天有 15 格而只需要放 3–4 条新的关键线索，余量足够铺这层保险。第 3 天自己那几条则不必再留后路。

不计入进度的**支线线索**（`linkedMainCase: false`）建议每天 2–4 条，它们的作用是喂剪辑台和播放量，漏掉不影响结局——大纲里"有一部分线索是不关键的"说的就是这批。

#### 要写多少内容

按上面排，三天一共 **34 格事件**（3 + 15 + 16），现在 `content/days.json` 里真正占得到格的是 11 格（第 1 天 3 + 第 2 天 4 + 第 3 天 4；工厂那两条事件互斥、只算 1 格，`e_finale_baby` 是串联出来的不占格）。也就是说正式剧本大约还要写 **23 条事件**，其中主线约 8–10 条、其余是支线和氛围事件。按地点摊下来，第 2/3 天每个开放地点平均排 2–3 层，正好是"同一个地方再去一次，东西变了"那套写法的自然容量。

> **待定（需要你拍板）：最后一格用满之后，走地图回加油站应不应该判死**。已实测确认现状：第 2 天 23:00 用掉最后一格走到 24:00 之后，点地图上的加油站**直接进"被黑夜吞噬"**（`ending` 当场写进存档），而点顶部菜单的"开始剪辑"则一切正常、照样能发布进下一天。两条路给出完全相反的结果，玩家没有任何办法预先知道区别在哪。三个方向：
> 1. **加油站永远可点**（推荐）：`onHotspotClick` 里把 `pendingDayOver` 的判定挪到"这个热点是不是调查地点"之后——回加油站是玩家在午夜时唯一该做的正确动作，不该因为动作做对了反而被判死。这样三天的可用格变成满额 4 / 16 / 16。
> 2. **堵掉顶部菜单那条路**：时间耗尽后禁用"开始剪辑"，把规则统一成"用满最后一格就是没赶回来"。这样可用格坐实为 3 / 15 / 15，上面的排布表按现状即可。
> 3. 维持现状不动，但在 UI 上给出明确提示（比如时钟剩最后一格时变红）。
>
> 在拍板之前，上面的排布表按方向 2（3 / 15 / 15）排，这样两个方向都不会出问题。

---

## 3. 系统清单

| 系统 | 状态 | 说明 |
|---|---|---|
| 图片地图 + 可点击热点 | 🔄 需重做 | 原型是按钮网格，改为真实地图图片上的坐标热点，见第 3.1 节 |
| **加油站基地点 + 信号系统** | 🆕 本轮新增 | 强制回归机制 + 被动粉丝留言触发，见第 3.3 节 |
| 时间系统 | ✅ 已有原型，窗口改为按天配置 | 与地图解耦（点击热点消耗时间的逻辑不变），每天的起止时间由内容数据指定，见第 2 节 3 天时间窗口表 |
| 线索采集 | ✅ 已有原型 | 沿用，需扩展为"主线/支线"两类标记 |
| 翻旧报纸/论坛老帖（原"居民热线"） | ✅ 已有原型 | 沿用，仅重命名，限定只能在加油站触发 |
| 剪辑并发布（原分开的"开始剪辑复盘"+"回加油站发布"，现已合并） | ✅ 已实现 | 简易剪辑软件 UI：素材库(当天已收集素材，按低/中/高重要度分档) + 时间轴；顶部"开始剪辑"菜单按钮和加油站"剪辑" tab 打开的是同一个编辑器，拖完点"确认发布"结算播放量、写入 publishLog（一天一次）；如果当天在 `days.json` 里配置了 `reviews`，确认发布时顺带判定时间轴顺序对不对，追加一段成功/失败反馈，写入 choiceLog 供结局内文案差异化用，但不影响播放量，见 `engine/review.js` + `engine/publish.js` |
| **掷骰事件（与理智值联动）** | 🆕 本轮新增 | 不做技能检定体系，见第 5.4 节 |
| 回加油站发布 vlog & 播放量结算（原"夜间播报"） | ✅ 已有原型 | 沿用，触发方式改为"点击加油站热点"而非纯时间到自动弹出 |
| 记事本 | ✅ 已有原型 | 沿用 |
| **关键词高亮联动档案库** | 🆕 本轮优先做 | 见第 4 节详细设计 |
| 跨天主案件追踪 | 🆕 需要 | 一个独立于"当日"的"主线线索簿"页面，显示目前拼出的主线进度 |
| 结局分支记录 | 🆕 需要 | 轻量：一个 `choiceLog` 数组记录关键选择，无需额外 UI |
| **定时全局事件（`timedEvents`）** | ✅ 已实现 | 到点必触发、与玩家所在地点无关，用于 1.6 节两次地震；`time.js crossedTimedEvents()` 做跨点检查，`main.js playTimedEvents()` 播出，复用现有事件弹窗与分页 |
| **地震演出** | ✅ 已实现 | `engine/quake.js` + ui.css：抖动 + 落灰 + 一拍死寂，按震级预设参数，零美术素材；预览工具 `tools/quake-preview.html` |
| **结局后日谈（`epilogue`）** | ✅ 已实现 | 结局分"正文 → 后日谈"两页揭晓，结局⑤的真相揭晓依赖它；`endings.json` 加字段 + `showEnding()` 分页播出 |
| **逐字/逐句浮现** | ✅ 已实现 | `engine/reveal.js` + ui.css `.rv-*`：结局标题逐字上浮、正文逐句淡入，地震事件同款；只用在这两处，普通事件正文照旧整段直出 |
| **折叠纸条演出** | ✅ 已实现 | `engine/paper.js` + ui.css「折叠纸条演出」一节：事件正文里配了 `paperSegments` 的那一页不进窗口，改成一张对折的纸条在屏幕中央翻开（上半块不动、下半块绕中间那道折痕翻回来，同时整张纸上抬四分之一做补偿，纸包不跑位），摊平之后字迹才逐句淡入，玩家点一下收起；零美术素材，字用系统楷体。第一处用在第 2 天早上旅馆前台那张宵禁留言（`e13`）；预览工具 `tools/paper-preview.html` |

### 3.1 图片地图系统（详细设计）

**交互方式**：地图是一张你提供的图片（整张小镇俯瞰图/手绘图均可），上面若干个位置以"热点标记"（比如一个可点击的图钉/光点）叠加显示，玩家点击标记进入对应地点，逻辑与现有的"点击地点消耗 30 分钟、检查事件表"完全一致，只是触发方式从按钮换成了图上的点。

**数据结构**：
```json
{
  "mapImage": "/assets/maps/town-map.png",
  "hotspots": [
    { "id": "gasStation", "name": "废弃加油站", "x": 5.8, "y": 52.5, "type": "basecamp", "icon": "gas" },
    { "id": "square", "name": "中心广场", "x": 42.5, "y": 61.0, "type": "investigation", "icon": "default" },
    { "id": "library", "name": "旧书馆", "x": 18.0, "y": 30.5, "type": "investigation", "icon": "default" }
  ]
}
```
- `x` / `y` 用**百分比坐标**（0-100，相对图片宽高），而不是像素坐标——这样地图在不同屏幕尺寸下缩放时，热点位置能跟着图片等比例移动，不会错位
- `type`：`"basecamp"`（只有加油站这一个）或 `"investigation"`（其余调查地点），引擎按这个字段区分渲染样式和点击后触发的逻辑分支（进基地=发布流程，进调查点=事件检查流程）
- `icon` 字段预留，方便以后给不同类型地点用不同图标（比如"已解锁新支线"的地点用感叹号图标）

**视觉状态**：热点标记需要区分几种状态，方便玩家一眼看出哪里还有事可做——
- 默认（未特别标记）
- 今日已去过（变暗/打勾）
- 有新事件可触发（轻微脉冲动效提示，不直接剧透具体内容，只提示"这里有点东西"）
- 主线相关地点（可选：用不同颜色的标记区分主线/支线地点）
- 加油站固定用不同的图标/配色（比如唯一的暖色高亮标记），让玩家在地图上一眼就能找到"回家的路"

**你需要准备的东西**：一张地图底图 + 每个地点大致想放的位置（包括加油站的位置，建议放在地图边缘，作为视觉上的"入口"）。坐标不需要你自己算——我可以做一个简单的"坐标拾取小工具"（上传图片、点击想要放标记的位置，直接读出 x/y 百分比），这样你标完点直接把坐标抄进 JSON 就行，不用猜数字。

### 3.2 缩放与平移

**结构**：`视口容器（overflow:hidden，固定屏幕区域）` → `内容层（地图图片 + 全部热点标记，一起做 transform）`。缩放/平移只需要改内容层的 `translate(x,y) scale(z)`，热点坐标是百分比、和图片绑定在同一层，永远保持对齐，不需要单独换算。

**交互**：
- 移动端：双指捏合缩放、单指拖拽平移
- 桌面端：滚轮缩放、鼠标拖拽平移
- 补充 `+` / `−` 按钮，覆盖不便手势操作的场景

**约束**：
- 接管手势自己实现缩放，不依赖浏览器原生整页缩放（避免状态栏/底部 tab 等 UI 被一起放大）
- 区分"拖拽平移"与"点击热点"：拖动位移小于阈值才判定为点击，否则判定为平移，防止拖图时误触发进地点
- 缩放范围建议限制在 1x-3x，平移范围限制在地图边界内，避免拖出可视区域后找不回来

### 3.3 加油站基地点与信号系统

**核心规则**：全镇默认无信号（`state.signal = false`），只有加油站范围内信号稳定。这不是氛围装饰，而是驱动第 2 节"每日必须走回加油站"这个空间约束的底层机制。

**加油站的两个功能**：
1. **发布点**：点击加油站热点触发"剪辑发布 vlog"流程（复用已有的新闻/播报稿件选入播出位→计算播放量的逻辑，只是入口从"任意时刻点开 tab"变成"必须先走到这个坐标"）
2. **免费查资料点**：原"居民热线"重命名后的"翻旧报纸/论坛老帖"限定只能在加油站触发（毕竟没信号也刷不了论坛），逻辑不变，一天一次

**信号闪现（被动随机事件，替代原来主动付费的"呼叫阿福"）**：
- 玩家在镇内探索、点击调查地点时，除了正常的事件判定，额外做一次低概率判定（建议 15-20%）：命中则触发"信号闪现"——屏幕上弹出 1-2 条刚收到的粉丝留言/评论通知，纯文本浮层，不打断探索节奏
- 数据结构：
```json
{
  "id": "sig3",
  "day": 5,
  "text": "@夜观星象:等等，我记得这个加油站，我爷爷说他年轻时经过这里再也没敢走小路......",
  "flavor": true,
  "clue": null
}
```
  - 大部分信号闪现是纯氛围文本（`flavor: true`），少数可以挂 `clue` 字段，作为"细思极恐"式的隐藏线索来源，甚至可以埋一些后期回看才明白的伏笔
  - 越到后期（第 1 节"发展与转折"阶段，即第 2、3 天），信号闪现的内容可以从"善意提醒"逐渐变成"诡异到不像正常网友会说的话"，呼应第 3 天"评论区出现自己没说过的话"的转折设定
- 当天所有信号闪现的留言，会在回到加油站发布时汇总展示一遍（"当日评论区"小结），给玩家一个统一回顾的时机，而不是错过了就没了

---

## 4. 关键词高亮联动档案库（详细设计）

### 4.1 交互方式
- 事件文本、新闻稿、甚至档案库词条正文中，特定名词会被标记为"可点击关键词"（原游戏里的蓝字）
- 点击后：
  1. 若该词条已解锁 → 直接跳转/展开档案库对应条目
  2. 若该词条**未解锁** → 消耗一次"关注"动作，将其解锁并展开（这是词条解锁的**主要途径之一**，与"事件自动解锁"并列）
- 已解锁的关键词在正文里持续保持高亮，未解锁的词也可以标记出来但呈现"灰底/虚线"提示"这里有可深挖的信息"，鼓励玩家点击

### 4.2 数据结构设计
每条正文用轻量标记语法书写，避免手写 HTML：

```json
{
  "id": "e2",
  "day": 2,
  "time": 600,
  "loc": "library",
  "text": "书架深处传来压低的争执声，隐约听见\"[[违约金|contract_penalty]]\"和\"[[雾湖|fogLake]]\"两个词。",
  "clue": "contract"
}
```
`[[显示文字|档案库key]]` 语法在渲染时被引擎解析成可点击 span，`档案库key` 对应 `archive.json` 里的条目 id。

### 4.3 档案库条目结构（支持嵌套关联）
```json
{
  "fogLake": {
    "title": "雾湖",
    "category": "地点",
    "body": "镇子西侧的天然湖泊……[[旧教堂|oldChurch]]的钟楼据说沉在湖底。",
    "unlockedBy": ["event:e2", "keyword_click"],
    "linkedMainCase": true
  }
}
```
- `category`：地点 / 人物 / 事件 / 物品，方便档案库分类 tab（可以在原有"地点/人物"分类基础上直接扩展）
- `linkedMainCase`：标记是否计入"主线进度"，用于第 3 节的跨天追踪页面
- 档案库正文本身也可以包含 `[[]]` 语法，实现词条之间互相跳转，形成一张越挖越深的关系网——这是 COC 调查感的关键，建议作为这套系统的设计重点

### 4.4 反馈与激励
- 每解锁一个关键词，给一次轻量正反馈（比如已有的 `📎 档案库已更新` 提示 + 一个小音效位）
- 可选：档案库首页显示"已收集 X / Y 条主线相关词条"的进度条，制造"越挖越接近真相"的驱动力

---

## 5. 理智值系统（已确认：深度模式）

理智值不只是氛围装饰，而是要**实际影响视频发布效果、并解锁隐藏剧情分支**。设计如下：

### 5.1 基础数值
- `state.sanity`：初始 100，全程不重置（跨天累积，这是核心——玩家的精神状态是"一条命"，不是每日重置的资源）
- 部分线索/事件带 `sanityCost` 字段，采集时直接扣减
  - 日常向线索（债务、感情纠纷）：`sanityCost: 0`
  - 主线怪异线索（读不懂的记录、不该存在的痕迹）：`sanityCost: 5~15`，视诡异程度分级
- 允许极少数"回复"手段（比如某天选择"什么都不做，只休息"，或某个 NPC 事件带来安慰），避免数值只降不升导致后期玩家躺平不敢点线索

### 5.2 三档阈值与效果
| 区间 | 状态标签 | 效果 |
|---|---|---|
| 70-100 | 清醒 | 正常游玩，视频发布效果按已有规则计算 |
| 35-69 | 动摇 | 部分事件文本出现"你不确定这是不是真的拍到了"式的不可靠叙述；档案库个别词条出现**第二版本**的诡异释义（原义 vs 理智值低时看到的"真"释义）；剪辑发布时有小概率"素材出错"，轻微影响当晚播放量 |
| 0-34 | 濒崩 | 解锁**仅在此区间可见的隐藏关键词/隐藏案件复盘选项**（即"疯狂中窥见真相"）；发布的素材会自动带出更接近真相但更危险的内容——播放量可能反而更高（观众爱看猎奇/惊悚素材），但会加速触向"黑暗结局" |

### 5.3 结局判定：五结局模型（已确认，替换旧的三结局模型与更早的 2x2 矩阵）

最早是"探索进度 × 最终 San 值"2x2 矩阵算 4 个结局，之后改成"San 值熔断 + 进度 + 一次高潮抉择"的三结局模型。现按 `content/drafts/outline.md` 里确认的大纲改为**五个结局**，判定轴换成三条，**San 值不再参与任何一条结局分支的判定**（它仍然影响掷骰取值、低理智演出、文案变体，只是不再决定走哪个结局）：

| # | 结局 | id（`content/endings.json` 的 key） | 达成条件 | 结果 |
|---|---|---|---|---|
| ① | 被黑夜吞噬 | `night_madness` | 任意一天走到当天 `endMin`（午夜）时还没剪辑发布并回旅馆 | 游戏失败，当场结束，不进入下一天 |
| ② | 一无所知 | `oblivious` | 三天走完，关键线索没收齐（或收齐了却没去按那个抉择） | 安全离开小镇，收获几条烂 vlog |
| ③ | 神的降临 | `god_arrival` | 关键线索收齐 → 抉择：帮镇民**摧毁**子午线仪 | 小镇被放回现实世界、引发地震；眷属从时间裂隙里走出来，最后看见的是白湖上逐渐破茧的格拉基胚种 |
| ④ | 旧日应当留在过去 | `past_stays_buried` | 关键线索收齐 → 抉择：**修复**子午线仪，**不**救婴儿 | 古神将临的关键时刻封印被重新加固，小镇在不稳定的时间裂隙里被彻底摧毁，引发地震 |
| ⑤ | 于毁灭中拯救 | `salvation_in_ruin` | 关键线索收齐 → 抉择：**修复**子午线仪，**并**救婴儿 | 在镇民的劝说下，于封印加固前开一道口子接住一个婴儿。两人以为打出了完美结局——后日谈揭晓婴儿就是格拉基的化身 |

**三条判定轴**：

1. **有没有在午夜前剪辑发布并回旅馆** → 没有就是结局①。这条是即时判定，不等全程走完，也不看进度和 San 值。判定时机是"过了十二点之后玩家再次点地图的那一刻"，跨过十二点的那次交互本身照常走完，见 `engine/main.js` 的 `handleDayOver()` / `onHotspotClick()`。
2. **关键线索有没有收齐** → 没收齐就是结局②。"关键线索"= `content/archive.json` 里 `linkedMainCase: true` 的词条，达标线是**全部**（`engine/ending.js` 的 `FINALE_PROGRESS_THRESHOLD = 100`，嫌苛刻就调这一个数）。**有一部分线索是不关键的**（`linkedMainCase: false`），漏掉不影响结局——这正是这个字段的用处。
3. **高潮抉择（两问，有先后）** → 结局③/④/⑤。位置在第 3 天的 `alkaliWorks`（子午线仪就封存在废弃工厂地下），准入门槛是第 2 条已达标：
   - **第一问**：镇民提议"修复还是摧毁子午线仪"。选摧毁 → 当场定为结局③；
   - **第二问（只在选了修复之后才出现）**：镇民接着劝"在加固封印之前先开一道口子，把镇里唯一无辜的婴儿放出来"。接受 → 结局⑤；拒绝 → 结局④。

**真值表**（`engine/ending.js` 的 `resolveFinale()` 就是这张表）：

| `finale.meridian` | `finale.baby` | 结局 |
|---|---|---|
| `'destroy'` | 不问 | `god_arrival` |
| `'repair'` | `true` | `salvation_in_ruin` |
| `'repair'` | `false` | `past_stays_buried` |
| `'repair'` | `null` | 还没定——等第二问 |
| `null` | 任意 | 还没定——没走到抉择 |

> **实装状态：✅ 已完成**。
> - `engine/ending.js`：`ENDING` 五个 id、`FINALE_PROGRESS_THRESHOLD`、`mainCluesComplete()`、`resolveFinale()` 真值表、`applyFinaleChoice()`、`decide()`（兜底给 `oblivious`）。
> - `engine/main.js`：新增事件类型 `type: 'choice'`——正文照常按换行分页，最后一页的"继续"换成一组选项按钮；选项可带 `finale`（交给真值表）、`tag`（写进 `choiceLog`）、`next`（串到同一天 `events` 里另一条事件，大纲里"选了修复才被劝救婴儿"就是这么串的）以及普通事件的效果字段。抉择窗口**关不掉**（`renderWindow` 的 `noClose`），且要等玩家真的选了才记进 `triggeredEvents`——中途刷新页面不会把高潮弄丢。
> - 事件新增可选的 `requires` 门槛（`allMainClues` / `clues` / `archives`），引擎取该地点第一条满足门槛的事件，所以同一个地点可以并排摆"够格才给的那一版"和"不够格时的兜底版"。第 3 天的 `alkaliWorks` 就是 `e_finale` + `e_finale_locked` 这对。
> - 抉择一旦定下结局，游戏**当场**进结局窗口（不再回旅馆睡觉、不再走第 3 天的发布流程）。

**⚠ 写作注意：五条路里有四条通向格拉基降临**——①封印自行碎裂、②同样自行碎裂只是没人知道、③她们亲手放它出来、⑤胚种随婴儿出逃。**唯一真正拦下它的是④，代价是整座镇子和里面所有人**。玩家改变得了结果，但改变得了的那一次要付出的正是"救不了任何人"，这是有意为之的宇宙恐怖内核。必须正面处理的副作用还是那个：**如果玩家觉得"选什么都一样"，进度和抉择就失去意义了**。处理方向：

- **差别不在后果，在"谁知道"**：③是主角亲眼看着灾难发生；⑤是主角带着一场胜利开车离开，**只有玩家知道**；②是主角和玩家都不知道。这是一条"知情度"的梯子，而探索进度决定你能爬到哪一级——这就是进度的真正回报，不是更好的结局，是更清楚的视野。④是唯一一级"知道了，也做对了"，所以它必须写得最疼。
- **④和⑤是同一个选择的两面**，这是整套抉择最值钱的地方：它们前面的那一步完全一样（修复子午线仪），只差最后那一道口子开不开。玩家选⑤的理由（至少救一个孩子）正是它变成最糟结果的原因；玩家选④的理由（不信任任何从那道缝里出来的东西）正是它成为唯一正解的原因——**而游戏永远不会当面告诉玩家哪个是哪个**。
- **结局⑤靠"正文 / 后日谈"两段式兑现**（已确认，写法见 1.4 节）：正文给一场毫无破绽的胜利，后日谈揭晓婴儿是什么。真正的落差发生在这两段之间，而不是靠在正文里埋暗示——暗示会提前泄掉那口气。
- **②的收尾不该有"揭晓"**，它的恐怖恰恰是没有揭晓：两人平安开走，新闻里一条 7.1 级地震，就这样。玩家若在别的周目走到过③④⑤，回头再看②才会明白那条新闻是什么。
- **`variants` 退居次要**：仍可按 `choiceLog` / 状态给正文追加细节，但它只负责"揭晓得多细"，不负责"揭不揭晓"。`when` 除了匹配 `choiceLog` 里的 tag（剪辑复盘的 `successTag`/`failTag`、抉择选项的 `tag`），还能匹配引擎按状态算出来的伪 tag（统一 `__` 前缀，见 `ending.js` 的 `computeStateTags`）：`__cluesComplete` / `__cluesIncomplete` / `__finaleSkipped`（线索收齐却没按下抉择）/ `__sanityBreaking` / `__sanityClear` / `__day1`~`__day3`。
- **San 值从判定里退出之后**（已确认）：5.1/5.2 节那套"熔断线"不再存在，游戏中途不会因为 San 值归零强制终局。San 值现在的全部作用是过程性的——掷骰取值规则（5.4 节）、低理智演出、隐藏选项、以及上面那些文案伪 tag。如果之后想把"疯掉"重新做成一个独立结局，它应该是**第六个**结局、走 `option.ending` 那个后门或另一条即时判定，而不是塞回这三条轴里。

- 关键线索达标线（`FINALE_PROGRESS_THRESHOLD`）是唯一一个还需要按真实内容量回调的数，改它不影响这套判定结构。**改内容时要留意的连带约束**：每个 `linkedMainCase` 词条都必须在"玩家一定走得到的正文"里配一个 `[[显示文字|key]]` 链接（词条只能靠点链接解锁），掷骰事件的**四档结果都要给**——只在成功档给链接的话，掷骰失败一次就等于永久锁死③④⑤。

### 5.4 掷骰事件（与理智值联动，不做完整技能检定体系）

部分事件不是"点了就一定触发固定文本"，而是标记为**掷骰事件**：抵达地点后弹出一次掷骰动画（建议用 1d20，视觉上是一个转动的骰子，比抽象概率条更有 COC 氛围），根据结果落入不同结果分支。**不引入角色卡/技能值**——骰子本身是公平的 1d20，真正影响结果分布的是理智值状态，这样不用额外做一套成长系统，又能让"精神状态"这条暗线渗透到几乎每一次判定里。

**数据结构**：
```json
{
  "id": "e9",
  "day": 6,
  "loc": "lake",
  "time": 690,
  "type": "diceCheck",
  "text": "唱诗席后面的地板缝隙里，似乎卡着什么东西……",
  "diceThreshold": 12,
  "outcomes": {
    "critFail": { "text": "你的手被什么东西划破，钻心地疼——但那里什么都没有。", "sanityCost": 8 },
    "fail":     { "text": "你只摸到一把冷灰，什么线索都没找到。", "sanityCost": 0 },
    "success":  { "text": "指尖碰到一枚生锈的胸针，边缘刻着几个已经磨平的字母。", "clue": "brooch" },
    "critSuccess": { "text": "胸针背面还留着半张没烧完的字条，字迹依然清晰。", "clue": "brooch_full", "sanityCost": -5 }
  }
}
```

**理智值如何影响掷骰（核心机制）**：骰子本身永远是 1d20 均匀分布，理智值不改变骰子，而是改变"取几次骰子结果、取哪一个"——理智越低，结果越容易滑向极端（大成功或大失败），呼应"精神状态越不稳定，行为和感知就越难预测"：

| 理智区间 | 掷骰方式 | 效果倾向 |
|---|---|---|
| 清醒（70-100） | 掷 1 次，直接取结果 | 标准概率分布，`critFail`/`critSuccess` 都很罕见（各约 5%） |
| 动摇（35-69） | 掷 2 次，取**离 10.5 更远**的一个 | 大成功/大失败概率同时上升，日常的"平庸成功/失败"变少——不稳定不等于变差，只是变得极端 |
| 濒崩（0-34） | 掷 3 次，取**离 10.5 最远**的一个；额外提供"不掷骰，直接判定为大成功"的隐藏选项 | 极端结果概率进一步升高；隐藏选项让玩家可以主动选择"不计代价换取真相"，但选择后必定伴随较高 `sanityCost`，把两人推得更深地留在濒崩档——极端骰面更多、演出更凶、隐藏内容更多，但两人对镜头前发生了什么也更不可靠（San 值本身已不再决定结局，见 5.3 节）|

**与其他系统的关系**：
- `critFail` / `critSuccess` 的结果同样可以带 `sanityCost`（正数扣、负数补），沿用 5.1 节的理智值增减机制，不需要新字段
- `success` / `critSuccess` 拿到的线索，可以和已有的关键词高亮语法（第 4 节）组合使用，`outcomes` 里的 `text` 同样支持 `[[显示文字|key]]` 标记
- 骰子结果本身也可以写入 `choiceLog`（比如"e9 掷出了 critFail"），但不直接决定走哪个结局——结局按 5.3 节那三条轴算。骰子对结局的唯一影响是间接的：**掷输了可能拿不到某条关键线索**，而关键线索收不齐就只剩结局②。所以掷骰事件的四档结果都必须给出 `linkedMainCase` 词条的 `[[..|key]]` 链接，输赢的差别放在拿不拿得到 vlog 素材上，不要放在词条能不能进记事本上

### 5.5 UI 提示
- 状态栏新增一个理智值指示（不用具体数字，用更氛围化的方式呈现，比如画面轻微噪点/文字轻微抖动随理智值降低而加剧，呼应手机拍摄/录屏质感的整体视觉风格）
- 具体数值可以在"记事本"或"档案库"里以隐藏方式透露给愿意深挖的玩家，而不是直接摆在主界面上，保留一点未知感

### 5.6 走到结局之后：时间线回退（已确认）

五个结局里有四个是一次性的、不可逆的判断（尤其④和⑤只差最后那一道口子开不开，见 5.3 节），但**看过一个结局的玩家最该做的事是去看另一个**——5.3 节那条"知情度的梯子"只有玩家把几条路都走过才立得住。所以走到**任何一个**结局（含①被黑夜吞噬）之后，结局窗口最后一页除了"重新开始整个旅程"，还给两条回头路：

- **从某个时间点继续**：按天列出这一局走过的每一个整点（"7月4日 14:00"，小字标着那一刻的理智和线索数），点一个就回到那一刻。粒度就是引擎的交互粒度——每次去调查地点固定花 1 小时（第 2 节），所以"每个整点"= 玩家做过的每一次选择的分岔口。
- **回到某个重大抉择**：列出这一局做过的抉择（是否摧毁子午线仪 / 是否要帮助婴儿），回去的是**选项还没摆出来**的那一刻，那条抉择事件会从正文开始重新播一遍。这是给"只想换一个答案、不想再跑一遍第 3 天"的玩家的快捷入口，也是④⑤两个结局之间唯一体面的往返方式。

**回退的转场：监控信号异常（已确认）**。点下清单里任何一个点，都先播一段画面断信号的演出——雪花、扫描线、横向撕裂带，角上打着 `● REC` 和 `CAM 01`，正中间那行时间码先乱跳，最后锁定成回到的那一刻（"7月4日 10:00"）。两层理由：一是**画面语言对得上**——这个游戏从头到尾是"被记录下来的东西"（vlog 素材、剪辑台、手机数据页，5.5 节那套录屏质感），玩家回到某个时间点，动作上等于把这卷带子倒回去重录；scan.js 那套取景框已经把"有个东西在隔着地图量这个镇子"摆在明面上了，监控画面这一层不是新加的比喻。二是**它遮住了换挡那一下**：地图重画、时钟跳表、结局窗口关掉全发生在雪花后面，玩家看到的是"信号回来的时候已经是另一个时刻了"，而不是界面当场闪一下变了样——回退这件事本来最容易露出"这是个存档系统"的马脚，这段演出正好把它按回故事里。

**实现口径**：两者是同一套东西的两种排法——`engine/state.js` 维护一条按时间排的**存档点链**，每天出发记一份、每次去调查地点（时间往前走之前）记一份、每个抉择事件摆出选项之前记一份，每份都是那一刻的完整状态快照。回退 = 把某一个快照整份还原回来，**回退点之后的存档点一并丢弃**（那条时间线已经改写，留着只会误导）。地图状态栏那个"时间线"按钮（"重新度过今日"/"回到上一天"）用的是同一条链上 `kind:'day'` 的那几个点。

> **实装状态：✅ 已完成**。`engine/state.js` 的 `checkpoints` 链 + `snapshotDay/snapshotTime/snapshotChoice` 与 `restoreDayCheckpoint/restoreCheckpoint`；`engine/main.js` 的 `openEndingRewind()`（两份清单共用一个窗口）+ `applyCheckpointRewind()`（抉择点还原后把那条事件重播一遍——高潮第二问 `e_finale_baby` 不挂在任何地点上，不重播就再也回不去了）；转场是 `engine/glitch.js` + `ui.css`「监控信号异常」一节，零美术素材（雪花/撕裂/滚动同步带全是 CSS 渐变按 `steps()` 跳位），换状态挂在它的 `onCut` 上，调参用 `tools/glitch-preview.html`。内容侧只需要每天写一个 `date`（"7月3日"）和给抉择事件写一个 `rewindLabel`（"是否摧毁子午线仪"），都不写也能跑，只是清单上显示成"第 N 天"和事件标题。

---

## 6. 已确认：节奏与结局规模

- **主线/支线节奏**：每天固定 1 条主线线索 + 1-2 条支线，作为默认模板贯穿全程；不额外设计"喘息日"机制，跑通前几天内容后如果节奏有问题再局部调整
- **结局数量**：已确认固定 **5 个结局**（见 `content/drafts/outline.md`），按 5.3 节那三条轴判定：午夜未归 → ①；关键线索没收齐 → ②；收齐后由高潮抉择的两问（砸/修、救不救婴儿）给出 ③/④/⑤。不再是矩阵式的 3-4 格，也不再有 San 值熔断那条路

---

## 7. 下一步（Claude Code 项目初始化清单）

设计框架已确认，进入 Claude Code 后按以下顺序搭建：

1. 项目结构：`engine/`（时间系统、地图、加油站/信号、复盘、发布/播放量、理智值、掷骰、关键词解析）+ `content/`（示例数据模板，非正式剧本）+ `assets/maps/`（你的地图图片）+ `design-doc.md`（本文档）
2. 把现有原型的引擎代码拆分重构，抽出通用逻辑，新增：
   - `map.js`：图片地图 + 百分比坐标热点渲染、点击交互、状态展示（重做，替换原按钮网格实现）
   - `signal.js`：信号闪现随机判定、加油站强制回归/超时惩罚逻辑，见 3.3 节
   - `sanity.js`：理智值状态、阈值判定、UI 反馈
   - `dice.js`：1d20 掷骰动画 + 按理智区间决定取骰次数/取值规则，见 5.4 节
   - `keyword-parser.js`：解析 `[[显示文字|key]]` 语法，渲染可点击 span
   - `archive.js`：支持 `unlockedBy` 多途径解锁、词条互相跳转
   - `ending.js`：✅ 已按 5.3 节的五结局模型实现——`ENDING` 五个 id、`mainCluesComplete()` 门槛、`resolveFinale()` 真值表、`applyFinaleChoice()`、`decide()` 兜底；高潮抉择本身是 `main.js` 里的 `type:'choice'` 事件，`choiceLog` 仅用于同一结局内的文案差异
3. 做一个独立的**坐标拾取小工具**（上传地图图片、点击取 x/y 百分比坐标），方便你后续自己往地图上加点，不需要每次都来问我坐标
4. 用占位文案填 3 天示例数据，跑通"从加油站出发 → 探索 → 信号闪现 → 线索 → 复盘 → 走回加油站发布vlog"全流程，验证引擎本身没问题，之后你就可以把 `content/` 换成自己的真实故事，独立开发，不再依赖我逐天写内容

本文档后续如有设计变动，直接在 Claude Code 项目里的 `design-doc.md` 增量修改，保持它是唯一的设计基准（single source of truth）。
