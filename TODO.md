# TODO —— 还没做的事

一份"欠账清单"，按**缺美术素材** → **缺界面/引擎** → **缺内容** 分三块。
做完一条就把 `- [ ]` 改成 `- [x]`，或者直接删掉那行。

设计上的详细论证在 `design-doc.md`，结构/字段说明在 `README.md`，这里只记"还差什么"。

---

## 一、缺的美术素材

现在游戏里除了两张地图（`assets/maps/`），**没有任何美术素材**，所有图标都是内联 SVG 或 emoji 顶着。
下面按"换上去最提气 → 可有可无"排。图给我就行，字段和接线我来加。

### 🔴 高优先级

- [ ] **每期视频封面 ×3**（发布后的手机数据页顶部）
  - 现状：拿卫星地图 `white-lake-map-now.png` 压暗顶着，一眼能看出是地图不是视频
  - 规格：竖版 **1080×1200 以上**，jpg / webp，不用透明
  - 注意：显示区 390×430、`object-fit: cover`、上边缘对齐，所以**主体别贴顶**；底部约 30% 会被渐变吃掉，别放重要内容；会再过一层压暗 + 暖色滤镜，**原图别自己压太黑**
  - 放 `assets/vlog/cover-d1.jpg` / `-d2` / `-d3`，我给 `content/days.json` 加 `vlogCover` 字段按天取
  - 先给 1 张通用的也行

- [ ] **三宫格数据图标 ×4**（播放 / 点赞 / 评论 / 关注者）
  - 现状：扁平 SVG + 橙色渐变方块，比参考图那种 3D 小物件差一截
  - 规格：**252×252 PNG，透明底**（显示 84×84）
  - 注意：**别自带深色背景板**，卡片本身是半透明的
  - 题材建议贴故事：旧 DV / 爱心徽章 / 留言纸条 / 小人像
  - 放 `assets/vlog/icon-*.png`

### 🟡 中优先级

- [ ] **素材缩略图**（「最热片段」卡左边那块）
  - 现状：胶片 SVG 占位
  - 两种做法二选一：
    - **省事版**：按重要度给 3 张（high / mid / low），324×324 PNG 透明底
    - **讲究版**：`content/materials.json` 里每条素材一张（目前 18 条，正式剧本后还会涨）——建议等剧本定稿再做
  - 我会给 `materials.json` 加 `thumb` 字段

### 🟢 低优先级 / 可有可无

- [ ] **月桂枝**（频道名左右两侧）—— 现在是内联 SVG，能用，就是叶子略机械。要换的话：约 **160×220 PNG 透明底**，只给左枝，右枝我镜像
- [ ] **奖杯图标**（顶部「N 期 · 本期数据」胶囊）—— 现在是 emoji 🏆，Windows 上是彩色的，跟暖色调略冲。**54×54 PNG 透明底**的单色小奖杯就能统一
- [ ] **游戏 favicon / 标题图**—— 现在浏览器标签页是空白图标

### 不用给的

关闭 ✕、评论气泡、shuffle 三个小图标（内联 SVG 够用）；评论区头像（首字母 + 渐变圆反而更像真评论区）。

---

## 二、缺的界面 / 引擎

- [ ] **手机顶部假状态栏**：加一条 iOS 风格状态栏，信号显示**「无服务」**——白湖镇本来就只有加油站有信号，这个细节很值。纯 CSS/SVG 不用出图，说一声我就加
- [ ] **字体**：prompt 里提到的 Neue Haas Unica / Helvetica Now Display 是商业字体，仓库里没有。有授权的 woff2 就扔进 `assets/fonts/`，我接上；现在走系统栈（中文落苹方/微软雅黑）
- [ ] **结局模型重构**：`engine/ending.js` + `content/endings.json` 还是旧的"探索进度 × San 值 2×2 四结局"，设计已确认改成 3 结局新模型（San 熔断/进度不够 → 结局①，达标后由高潮抉择二选一给结局②/③），见 `design-doc.md` 5.3 节。等正式大纲到位后和内容一起改
- [ ] **San 值瞬时熔断钩子**：`design-doc.md` 5.3 节第 1 条要求 `sanityCost` 每次调整后立即检查、跌破阈值就强制终局，现在引擎里还没有这个钩子
- [ ] **游戏名在页面内可见**：`index.html` 的 `<title>` 已是正式名，但 `#status-bar` 里没有展示位（见 `content/worldbuilding.md` 末尾）

---

## 三、缺的内容（`content/` 还是占位示例）

- [ ] **正式剧本**：`content/days.json` 现在整份是"违约金 / 看守人 / 胸针"那套占位剧情，等大纲到位后整体替换。写法走 `content/drafts/`（手机上写 `## [PENDING]` 段落，回电脑说一声"看一下草稿"）
- [ ] **核心神话真相落地**：已经确认的那套真相还只在对话里，没写进 `content/worldbuilding.md` / `content/archive.json`，也没有对应的主线事件
- [ ] **主角 / 频道正式命名**：QQ、BB 是占位名，现在手机数据页的频道名 `meta.channel` 直接写的 `QQ & BB`，定了名字这里一起改
- [ ] **六个新点位还是空的**：`hotel` / `tavern` / `museum` / `highSchool` / `alkaliWorks` / `theSink` 只有坐标和显示名，没有事件挂上去，也不在任何一天的 `unlockedLocations` 里
- [ ] **制碱公司 / 山谷的情节**：White Lake Alkali Company、White Lake Valley 两个已确认设定还没有任何词条和事件承接
- [ ] **vlog 评论区文案**：`days.json` 每天的 `vlogComments` 现在是占位示例，正式剧本时按天重写（`alien: true` 那条是"置顶不是我们发的"那种诡异评论，别浪费这个位置）
